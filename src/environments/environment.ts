// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyCaErLoi361t0qSdB8JZd1621bjEHh0vlE",
    authDomain: "fir-curdapp-17b2e.firebaseapp.com",
    databaseURL: "https://fir-curdapp-17b2e.firebaseio.com",
    projectId: "fir-curdapp-17b2e",
    storageBucket: "fir-curdapp-17b2e.appspot.com",
    messagingSenderId: "580407889963"
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
